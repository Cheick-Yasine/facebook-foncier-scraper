{
  "title": "Facebook Foncier Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Mode",
      "type": "string",
      "description": "Run mode",
      "enum": ["daily", "backfill"],
      "default": "daily",
      "editor": "select"
    },
    "daysBack": {
      "title": "Days back",
      "type": "integer",
      "description": "How many days of posts to collect",
      "default": 1,
      "minimum": 1,
      "maximum": 90
    },
    "groupLimit": {
      "title": "Group limit",
      "type": "integer",
      "description": "Maximum number of groups to process (0 = all)",
      "default": 0,
      "minimum": 0
    },
    "batchSize": {
      "title": "Batch size",
      "type": "integer",
      "description": "Number of groups per batch",
      "default": 5,
      "minimum": 1,
      "maximum": 20
    },
    "accountId": {
      "title": "Account ID",
      "type": "string",
      "description": "Facebook account identifier (1-5). Leave empty for default/single account.",
      "editor": "textfield"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Proxy settings (Apify Proxy or custom)",
      "editor": "proxy",
      "default": {
        "useApifyProxy": false
      }
    },
    "cookies": {
      "title": "Facebook cookies (JSON)",
      "type": "string",
      "description": "Exported Facebook cookies as JSON array (Cookie-Editor format accepted)",
      "editor": "textarea"
    },
    "skipLlm": {
      "title": "Skip LLM structuring",
      "type": "boolean",
      "description": "If true, only scrape raw posts without OpenAI enrichment",
      "default": false
    }
  },
  "required": []
}
